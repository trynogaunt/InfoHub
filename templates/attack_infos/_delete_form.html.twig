<form method="post" action="{{ path('app_attack_infos_delete', {'id': attack_info.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attack_info.id) }}">
    <button class="btn">Delete</button>
</form>
